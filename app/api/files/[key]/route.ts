import {getSupabaseAdmin,storageBucket} from "../../../../lib/supabase-admin";
export async function GET(req:Request,{params}:{params:Promise<{key:string}>}){const {key}=await params,requested=new URL(req.url).searchParams.get("name")||"",downloadName=requested.replace(/[\\/\r\n\0]/g,"_").slice(0,120)||true;const {data,error}=await getSupabaseAdmin().storage.from(storageBucket()).createSignedUrl(decodeURIComponent(key),300,{download:downloadName});if(error)return Response.json({error:error.message},{status:404});return Response.redirect(data.signedUrl)}
