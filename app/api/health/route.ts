import {getSupabaseAdmin} from "../../../lib/supabase-admin";
export async function GET(){const configured={supabaseUrl:Boolean(process.env.SUPABASE_URL),serviceRole:Boolean(process.env.SUPABASE_SERVICE_ROLE_KEY),storageBucket:Boolean(process.env.SUPABASE_STORAGE_BUCKET),openai:Boolean(process.env.OPENAI_API_KEY)};try{const {error}=await getSupabaseAdmin().from("materials").select("id",{head:true,count:"exact"});return Response.json({ok:!error,configured,database:error?error.message:"connected"},{status:error?500:200})}catch(e){return Response.json({ok:false,configured,database:e instanceof Error?e.message:"unavailable"},{status:500})}}
