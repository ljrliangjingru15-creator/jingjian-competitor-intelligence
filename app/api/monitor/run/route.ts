export async function POST(req:Request){const {organization,keywords,region}=await req.json() as Record<string,string>,key=process.env.OPENAI_API_KEY;if(!key)return Response.json({error:"请配置 OPENAI_API_KEY"},{status:503});const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${key}`,"content-type":"application/json"},body:JSON.stringify({model:"gpt-5.6-terra",tools:[{type:"web_search"}],input:`搜索${organization}最近30天在${region||"中国"}的可核实公开动态。关键词：${keywords}。返回标题、URL、来源、摘要、发布日期。`})});return Response.json(await r.json(),{status:r.status})}
