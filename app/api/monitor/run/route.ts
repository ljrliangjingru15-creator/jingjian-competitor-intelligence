import { env } from "cloudflare:workers";
export async function POST(req:Request){const {organization,keywords,region}=await req.json() as Record<string,string>;if(!env.OPENAI_API_KEY)return Response.json({error:"请配置 OPENAI_API_KEY"},{status:503});const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${env.OPENAI_API_KEY}`,"content-type":"application/json"},body:JSON.stringify({model:"gpt-5.6-terra",tools:[{type:"web_search"}],input:`搜索${organization}最近30天在${region||"中国"}的官网新闻、公开活动、高校合作公告和产品升级。关键词：${keywords}。返回最多8条，每条包含标题、URL、来源、摘要、发布日期。只用可核实公开来源。`})});const d=await r.json() as any;return Response.json(d,{status:r.status})}
